import { Cormorant_Garamond, Inter } from "next/font/google";

export const cormorantGaramondSerif = Cormorant_Garamond({
  variable: "--font-cormorant-garamond",
  subsets: ["latin"],
});


export const interSans = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});